# 手機時間存摺 — 使用與維護筆記

家裡的週末手機時間積分制度。孩子平日做家事、準時作息可以賺分，週末用分數換手機時間。
所有資料存在 Google 試算表，App 只是介面。

---

## 每天怎麼用

App 網址開啟後有兩個分頁。

### 小孩檢視區（不用密碼）

孩子自己看的。切換哥哥／妹妹不會重新連線，是即時的。

- 最上方大數字是目前積分，**負分會變紅色**並出現「目前負分了，請好好表現!」
- 下方卡片顯示可以換多少手機時間，**分數夠了整張卡會變綠色**
- 「本週積分紀錄」只顯示**本週日到週六**的明細，每週自動換頁

積分**不會每週歸零**，一直累積下去。週的分界只影響明細顯示。

### 家長操作區（密碼 **1234**）

登記加分、扣分、兌換都在這裡。步驟：

1. 選孩子
2. 選分類（每日任務／加分／扣分／兌換），下拉只會列出該分類的項目
3. 分數會自動帶入，可以手動加減
4. 備註選填
5. 送出

**兌換點數不足會被擋下來**，按鈕變灰並顯示還差幾點。這是刻意的 —— 不能預支。
扣分則**可以扣成負分**，表現不好欠著，之後賺回來。

密碼要改的話，在家長操作區右上角的鑰匙圖示。

---

## 計分規則

**T03「1小時內洗完澡、洗學校碗筷」只要做到其中一項就給滿 +2。**
沒做到的那一項不影響 T03，而是從對應的扣分項目另外扣（沒帶餐具扣 T19、沒準時洗澡扣 T21、學校碗筷沒洗扣 T15）。兩項都沒做到才不給 T03。

T07「洗自己早餐碗筷」跟 T03 無關，那是在家吃完早餐**主動**洗碗的額外加分。

兌換匯率：**10 點 = 30 分鐘**（1 點 = 3 分鐘）。這個比例是從配分表最便宜的兌換項目自動推算的，改配分表就會跟著變。

---

## Google 試算表結構

三個分頁，**欄位名稱不要亂改**（程式靠名稱找欄位，但可以自由調整欄位順序）。

| 分頁 | 欄位 |
|---|---|
| 任務與配分表 | TaskID、Category (類別)、Task_Name (任務名稱)、Default_Points (預設分數)、備註 |
| 點數存摺 | LogID、Timestamp (時間戳記)、UserID (對象)、Task_Name (項目)、Points (異動積分)、Current_Points (目前積分)、Note (備註說明) |
| 使用者資料與餘額 | UserID、Name (姓名)、Grade (年級)、Current_Points (目前積分)、密碼 |

孩子的總分以「使用者資料與餘額」的 `Current_Points` 為準，存摺每列的 `Current_Points` 是當下的餘額快照。

---

## 常見維護

**新增任務項目** — 在家長操作區選「自訂項目 ✨」，勾選「同時儲存至試算表」，送出後就會出現在下拉選單裡，不用重新載入。編號由伺服器自動接續（T24、T25…），不要自己編。

也可以直接在試算表新增一列，但 **TaskID 一定要照 `T` + 數字的格式連號**。填了像 `T8256` 這種數字，下一個自動編號就會變成 `T8257`。

**修改既有紀錄** — 直接在試算表改那一格。
只改時間、項目名稱、備註是安全的。**不要改分數欄** —— 改了會讓該列之後所有列的餘額快照和孩子總分全部對不起來，必須整條重算。

**清空重來** — 刪掉「點數存摺」除了標題列以外的所有列，並把「使用者資料與餘額」的 `Current_Points` 清空。兩邊要一起清。

---

## 遇到問題時

**App 顯示寫入失敗** — **先重新整理看資料在不在，不要直接重送。**
Google Apps Script 是「先執行、再回傳結果」，回傳那段偶爾會掉，但資料其實已經寫進去了。重送會變成兩筆、多扣一次分。

**App 顯示「已切換至快取模式」** — 連不到 Google 試算表。通常是網路問題，或公司內網擋了 `script.google.com`。畫面會用本機快取的舊資料，可以看但不要登記。

**改了 Apps Script 程式碼之後 App 整個連不上** — 多半是重新部署時按了「新增部署作業」，那會產生**全新的網址**，舊的立刻失效。
正確做法是：**部署 → 管理部署作業 → 點現有那筆 → 鉛筆圖示 → 版本選「新版本」→ 部署**。這樣網址不變。

如果不小心換了網址，在 App 右上角齒輪的設定畫面裡把新網址貼進去就能救回來。

---

## 本機開發

```bash
npm install     # 公司內網裝不起來，要切手機熱點（內網的 TLS 憑證太弱，Node 會拒連）
npm run dev     # http://localhost:3000
```

後端程式碼在本機的 `gas/Code.gs`（**沒有上傳到 GitHub，這是唯一的副本，別刪**）。
改了要手動貼到 Apps Script 並重新部署才會生效。

技術細節與部署方式見 `SYSTEM.md`。
